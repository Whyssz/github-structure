const arr = [1, 1, 2, 3, 'Dima', 'Dima'];
const set = new Set(arr);

// set.add('Oleg');
// set.has('Dima');
// set.delete('Dima');
// set.clear();
// set.size;

/* Funciton uniq arr
  const unique = (arr) => {
    return Array.from(new Set(arr));
  };
  console.log(unique(set));
*/

/* Enumirable ex
  for (let value of set) console.log(value)
  forEach(value, valueAgain, set) ...

  add: set.values // set.keys
*/
